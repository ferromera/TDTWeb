<%= provide(:title, 'Plantilla') %> 
<h1><%=@team.name%>: Plantilla</h1>
<table id="tablaPlantilla" class="sortable">
  <thead>
  <tr><th>Pos</th><th>Nombre</th><th>Valoración</th><th>Edad</th><th>Altura</th><th>Valor</th></tr>
  </thead>
  <tbody>
  <% @team.players.each do |p| %>
  <tr><td sorttable_customkey=<%= getPositionOrder p.registeredPosition %>><%= getPositionImage p.registeredPosition %></td>
  	<td id="NombrePlantilla"><%=link_to p.name, p%></td>
  	<td color=<%=get100StatColor p.overallRating %>><%=p.overallRating%></td>
  	<td><%=p.age%></td>
  	<td><%=p.height%></td>
  	<td sorttable_customkey=<%=getPrice p.overallRating%>>$<%=getPrice p.overallRating%>M</td></tr>
  <% end %>
  </tbody>
</table>
